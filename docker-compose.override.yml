version: '3.8'

services:
  auth-service:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder  # Use build stage for development
    volumes:
      - .:/app
      - /app/node_modules  # Prevent overwriting node_modules
    command: npm run start:dev  # Use development mode
    environment:
      - NODE_ENV=development
